{
  "name": "group21-big",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run start --prefix backend",
    "postinstall": "npm install --prefix frontend/big-project-web && npm install --prefix backend && npm run build --prefix frontend/big-project-web"
  },
  "cacheDirectories": [
    "frontend/big-project-web/node_modules",
    "backend/node_modules"
  ],
  "dependencies": {
    "react-duration-picker": "^1.1.1",
    "react-native-paper": "^4.7.2"
  }
}
